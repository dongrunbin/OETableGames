
//===================================================
//Author：DRB
//CreateTime：2021/4/28 16:56:01
//Remark：This code is generated by the tool
//===================================================
package com.oegame.tablegames.model.local.gen;
import com.oegame.tablegames.common.util.StringUtil;
import com.oegame.tablegames.model.local.*;

// Settings数据管理
public class SettingsDBModel extends AbstractDBModel<SettingsEntity>
{
    private static SettingsDBModel dbModel = new SettingsDBModel();
    public static SettingsDBModel singleton()
    {
        return dbModel;
    }
    private SettingsDBModel() { }
    // 文件名称
    protected String getFileName() {return "Settings.bytes";}

    /// 创建实体
    protected SettingsEntity makeEntity(LocalDataParser parse)
    {
        SettingsEntity entity = new SettingsEntity();
        entity.id = StringUtil.toInt(parse.getFieldValue("id"));
        entity.descrption = (parse.getFieldValue("descrption"));
        entity.gameName = (parse.getFieldValue("gameName"));
        entity.gameId = StringUtil.toInt(parse.getFieldValue("gameId"));
        entity.label = (parse.getFieldValue("label"));
        entity.name = (parse.getFieldValue("name"));
        entity.tags = (parse.getFieldValue("tags"));
        entity.mode = StringUtil.toInt(parse.getFieldValue("mode"));
        entity.value = StringUtil.toInt(parse.getFieldValue("value"));
        entity.cost = StringUtil.toInt(parse.getFieldValue("cost"));
        entity.status = StringUtil.toInt(parse.getFieldValue("status"));
        entity.selected = StringUtil.toBoolean(parse.getFieldValue("selected"));
        return entity;
    }
}
